DATABASE_URI=postgresql://postgres:postgres@localhost:5432/zun_cms
PAYLOAD_SECRET=super-secret-change-in-production
NEXT_PUBLIC_SERVER_URL=http://localhost:3000
REVALIDATE_URL=
REVALIDATE_SECRET=
# Production only — password for the Postgres Docker container
POSTGRES_PASSWORD=
# Required when running `pnpm seed` — set the initial admin account credentials
SEED_ADMIN_EMAIL=
SEED_ADMIN_PASSWORD=
